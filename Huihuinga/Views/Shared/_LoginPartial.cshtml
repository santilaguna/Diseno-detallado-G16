@using Microsoft.AspNetCore.Identity
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager


@if (SignInManager.IsSignedIn(User))
{
        <a  class="ticket" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage"><span>@User.Identity.Name!</span></a>
        <form  class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
            <button  type="submit" class="ticket"><span>Salir</span></button>
        </form>
}
else
{
    
        <a class="ticket" asp-area="Identity" asp-page="/Account/Register"><span>Registrarse</span></a>
        <a class="ticket" asp-area="Identity" asp-page="/Account/Login"><span>Ingresar</span></a>
}
